[defaults]
# Collections paths for offline mode (Docker populates chimera/ansible/collections)
collections_path = chimera/ansible/collections:~/.ansible/collections

# Roles paths for offline mode (Docker populates chimera/ansible/roles)
roles_path = chimera/ansible/roles:~/.ansible/roles

[ssh_connection]
# Reduce SSH timeout warnings
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
