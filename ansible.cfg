[defaults]
# Collections paths for offline mode (Docker populates spel/ansible/collections)
collections_path = spel/ansible/collections:~/.ansible/collections

# Roles paths for offline mode (Docker populates spel/ansible/roles)
roles_path = spel/ansible/roles:~/.ansible/roles

[ssh_connection]
# Reduce SSH timeout warnings
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
