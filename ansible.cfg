[defaults]
# Collections paths for offline mode (Docker populates granite/ansible/collections)
collections_path = granite/ansible/collections:~/.ansible/collections

# Roles paths for offline mode (Docker populates granite/ansible/roles)
roles_path = granite/ansible/roles:~/.ansible/roles

[ssh_connection]
# Reduce SSH timeout warnings
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
