# =============================================================================
# Prepare Offline Docker Image
# =============================================================================
# This workflow builds the Crucible Docker image with all dependencies baked in
# and exports it as a portable tarball artifact for use in air-gapped environments.
#
# PREREQUISITES:
#   - Dockerfile must exist in repository root
#   - Iron Bank credentials configured as GitHub secrets:
#     - IRONBANK_USERNAME: Robot account username
#     - IRONBANK_PASSWORD: CLI token (expires every 6 months)
#     - Renewal: https://registry1.dso.mil → User Profile → CLI Token → Regenerate
#
# OUTPUTS:
#   - Docker image tarball: crucible-builder-YYYYMMDD.tar.gz
#   - Artifact retention: 30 days
#
# USAGE:
#   The exported Docker image can be:
#   1. Downloaded from GitHub Actions artifacts
#   2. Transferred to air-gapped environment
#   3. Imported with: gunzip -c crucible-builder-*.tar.gz | docker load
#   4. Run with: docker run -v $(pwd):/workspace crucible-builder:<tag> make build
# =============================================================================

name: Prepare Offline Docker Image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Docker image tag (default: YYYYMMDD)"
        required: false
        default: ""
        type: string

permissions:
  contents: read

jobs:
  build-docker-image:
    name: Build and Export Docker Image
    runs-on: ubuntu-latest
    timeout-minutes: 30  # Docker build with all dependencies

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Set up environment variables
        id: config
        run: |
          # Use provided tag or default to date
          if [ -n "${{ github.event.inputs.image_tag }}" ]; then
            IMAGE_TAG="${{ github.event.inputs.image_tag }}"
          else
            IMAGE_TAG="$(date +%Y%m%d)"
          fi
          
          echo "IMAGE_TAG=${IMAGE_TAG}" >> $GITHUB_ENV
          echo "IMAGE_NAME=crucible-builder" >> $GITHUB_ENV
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          
          echo "=== Build Configuration ==="
          echo "Image: crucible-builder:${IMAGE_TAG}"
          echo "Date: $(date -u +%Y-%m-%dT%H:%M:%SZ)"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Iron Bank
        uses: docker/login-action@v4
        with:
          registry: registry1.dso.mil
          username: ${{ secrets.IRONBANK_USERNAME }}
          password: ${{ secrets.IRONBANK_PASSWORD }}

      - name: Build Docker image
        run: |
          echo "=== Building Docker Image ==="
          echo "Image: crucible-builder:${IMAGE_TAG}"
          echo ""
          
          # Build with buildx for better caching
          docker buildx build \
            --tag "crucible-builder:${IMAGE_TAG}" \
            --tag "crucible-builder:latest" \
            --load \
            --progress=plain \
            .
          
          echo ""
          echo "=== Build Complete ==="
          docker images crucible-builder

      - name: Verify Docker image
        run: |
          echo "=== Verifying Docker Image ==="
          
          # Run quick verification (bypass entrypoint to get clean output)
          docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" packer version
          docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" ansible --version
          docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" aws --version
          
          # Show image details
          echo ""
          echo "=== Image Details ==="
          docker inspect "crucible-builder:${IMAGE_TAG}" --format='{{.Id}}' | head -c 20
          echo ""
          docker inspect "crucible-builder:${IMAGE_TAG}" --format='Size: {{.Size}} bytes'
          docker inspect "crucible-builder:${IMAGE_TAG}" --format='Created: {{.Created}}'

      - name: Export Docker image as tarball
        run: |
          echo "=== Exporting Docker Image ==="
          
          TARBALL_NAME="crucible-builder-${IMAGE_TAG}.tar.gz"
          
          # Save and compress
          docker save "crucible-builder:${IMAGE_TAG}" | gzip > "${TARBALL_NAME}"
          
          # Generate checksum for binary tarball
          sha256sum "${TARBALL_NAME}" > "${TARBALL_NAME}.sha256"
          
          # Base64 encode for SharePoint transfer compatibility
          echo ""
          echo "=== Base64 encoding for SharePoint transfer ==="
          base64 "${TARBALL_NAME}" > "${TARBALL_NAME}.b64"
          
          # Generate checksum for base64 file
          sha256sum "${TARBALL_NAME}.b64" > "${TARBALL_NAME}.b64.sha256"
          
          echo ""
          echo "=== Export Complete ==="
          echo "Binary tarball (direct transfer):"
          ls -lh "${TARBALL_NAME}"*
          echo ""
          echo "Base64 encoded (SharePoint transfer):"
          ls -lh "${TARBALL_NAME}.b64"*
          echo ""
          cat "${TARBALL_NAME}.sha256"
          cat "${TARBALL_NAME}.b64.sha256"

      - name: Generate manifest
        run: |
          MANIFEST_FILE="crucible-builder-${IMAGE_TAG}-manifest.txt"
          
          cat > "${MANIFEST_FILE}" << EOF
          Crucible Builder Docker Image Manifest
          ===================================
          
          Image Tag: crucible-builder:${IMAGE_TAG}
          Build Date: $(date -u +%Y-%m-%dT%H:%M:%SZ)
          Git Commit: ${GITHUB_SHA}
          Git Ref: ${GITHUB_REF}
          Workflow Run: ${GITHUB_RUN_ID}
          
          Image Details:
          --------------
          $(docker inspect "crucible-builder:${IMAGE_TAG}" --format='Image ID: {{.Id}}')
          $(docker inspect "crucible-builder:${IMAGE_TAG}" --format='Size: {{.Size}} bytes')
          $(docker inspect "crucible-builder:${IMAGE_TAG}" --format='Created: {{.Created}}')
          
          Baked-in Tools:
          ---------------
          Packer: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" packer version 2>/dev/null | head -1)
          Ansible: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" ansible --version 2>/dev/null | head -1)
          Python: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" python3 --version 2>/dev/null)
          AWS CLI: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" aws --version 2>/dev/null)
          
          Tarball:
          --------
          $(ls -lh crucible-builder-${IMAGE_TAG}.tar.gz)
          
          SHA256 Checksum:
          ----------------
          $(cat crucible-builder-${IMAGE_TAG}.tar.gz.sha256)
          
          Usage:
          ------
          # Import the image
          gunzip -c crucible-builder-${IMAGE_TAG}.tar.gz | docker load
          
          # Run a build
          docker run --rm \\
            -v \$(pwd):/workspace \\
            -e AWS_ACCESS_KEY_ID \\
            -e AWS_SECRET_ACCESS_KEY \\
            -e AWS_DEFAULT_REGION=us-east-1 \\
            -e CRUCIBLE_BUILDERS=amazon-ebssurrogate.minimal-rhel-9-hvm \\
            -e CRUCIBLE_VERSION=\$(date +%Y.%m).1 \\
            crucible-builder:${IMAGE_TAG} make build
          EOF
          
          cat "${MANIFEST_FILE}"

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v6
        with:
          name: crucible-builder-${{ steps.config.outputs.image_tag }}
          # Both formats included for different transfer scenarios:
          # - .tar.gz: Direct transfers (GitHub→local, USB, SCP) - smaller, faster
          # - .tar.gz.b64: SharePoint transfers (avoids binary corruption) - larger, safer
          path: |
            crucible-builder-${{ steps.config.outputs.image_tag }}.tar.gz
            crucible-builder-${{ steps.config.outputs.image_tag }}.tar.gz.sha256
            crucible-builder-${{ steps.config.outputs.image_tag }}.tar.gz.b64
            crucible-builder-${{ steps.config.outputs.image_tag }}.tar.gz.b64.sha256
            crucible-builder-${{ steps.config.outputs.image_tag }}-manifest.txt
          retention-days: 30
          compression-level: 0  # Already gzipped

      - name: Create summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## Crucible Docker Image Build Summary
          
          | Property | Value |
          |----------|-------|
          | Image Tag | \`crucible-builder:${IMAGE_TAG}\` |
          | Build Date | $(date -u +%Y-%m-%dT%H:%M:%SZ) |
          | Git Commit | \`${GITHUB_SHA:0:8}\` |
          | Tarball Size | $(ls -lh crucible-builder-${IMAGE_TAG}.tar.gz 2>/dev/null | awk '{print $5}' || echo 'N/A') |
          
          ### Baked-in Tools
          - **Packer**: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" packer version 2>/dev/null | head -1 || echo 'N/A')
          - **Ansible**: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" ansible --version 2>/dev/null | head -1 || echo 'N/A')
          - **Python**: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" python3 --version 2>/dev/null || echo 'N/A')
          - **AWS CLI**: $(docker run --rm --entrypoint "" "crucible-builder:${IMAGE_TAG}" aws --version 2>/dev/null || echo 'N/A')
          
          ### Artifact
          Download the \`crucible-builder-${IMAGE_TAG}\` artifact to get:
          - Docker image tarball (portable, air-gap compatible)
          - SHA256 checksum for verification
          - Build manifest with usage instructions
          
          ### Next Steps
          1. Download the artifact from this workflow run
          2. Transfer to your build environment
          3. Import: \`gunzip -c crucible-builder-${IMAGE_TAG}.tar.gz | docker load\`
          4. Run builds using the \`build.yml\` workflow or manually with docker run
          EOF
